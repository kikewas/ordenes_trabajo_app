from setuptools import setup, find_packages

setup(
    name='ordenes_trabajo_app',
    version='0.0.1',
    description='App para gestión de órdenes de trabajo',
    author='kikewas',
    author_email='kikewas@example.com',
    packages=find_packages(),
    zip_safe=False,
    include_package_data=True,
    install_requires=[],
)
